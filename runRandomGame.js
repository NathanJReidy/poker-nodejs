const { playGame } = require("./poker.js");
const { array } = require("./poker.js");

// Runs a stream of 100 random poker games.
// Change 100 to however many games you want to run.
playGame(100, (runTest = false));

// Play game 100 times
// playGame(100);
