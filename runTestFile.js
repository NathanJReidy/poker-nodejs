const { playGame } = require("./poker.js");
const { array } = require("./poker.js");

// Runs the entire test file of poker games and returns the expected winner
playGame(array.length, (runTest = true));
